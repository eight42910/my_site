# my_site

## UI updates
- モバイルナビは `components/Header.tsx` と `components/MobileMenuOverlay.tsx` で構成。
- Productカードの見え方は `app/product/page.tsx` の `cardVariant` で切り替え可能。
- Worksカードは `content/works/*.mdx` の `outcome` を一覧に表示。
